type SmartItem = {
  columnName: string,
  value: any,
};

export default class ArticleItem {
  constructor(item: SmartItem) {
    this.column = kanji;
    this.pronunciation = pronunciation;
    this.meaning = meaning;
  }
}
